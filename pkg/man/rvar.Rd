\name{rvar}
\alias{rvar}
\title{compute the variances for each row in a matrix (e...}
\usage{rvar(x, n=ncol(x))}
\description{compute the variances for each row in a matrix (e.g. of expression values)}
\arguments{\item{x}{matrix}
\item{n}{number of columns in the matrix; defaults to ncol(x)}}
